# FlashFlow — AI Agent 开发规则

## 项目概述

- **名称**：FlashFlow
- **定位**：高并发实时交易平台（Java 后端秋招项目）
- **技术栈（规划）**：Java 21、Spring Boot、Spring Cloud、MySQL、Redis、Kafka、WebSocket、Docker；后期可能加入 Kubernetes

本文件约束后续所有 AI 辅助开发行为。未明确要求时，不得创建 Spring Boot 工程或业务代码。

---

## 开发节奏

1. **一次只完成一个需求**  
   每个任务只实现当前明确要求的功能点，交付后停止，等待下一指令。

2. **禁止提前开发后续功能**  
   不得预埋未要求的模块、接口、配置或“顺便”实现后续计划中的能力。

3. **修改代码前先阅读现有代码**  
   改动前必须阅读相关已有代码与调用链，基于现状修改，禁止凭假设直接写。

4. **禁止随便增加依赖**  
   新增 Maven/Gradle 依赖须有明确必要性，并说明用途；能用现有依赖解决则不加。

5. **禁止修改与当前需求无关的文件**  
   不重构、不“顺手清理”、不改无关配置或文档（本文件被明确要求时除外）。

---

## Java / Spring 约定

6. **使用构造器注入**  
   Spring Bean 通过构造器注入依赖；禁止字段 `@Autowired` / `@Resource` 注入（测试中确有惯例除外，需说明）。

7. **Controller 不写业务逻辑**  
   Controller 只做参数校验、鉴权入口、调用 Service、组装响应；业务逻辑放在 Service（或领域层）。

8. **DTO、VO、Entity 分离**  
   - Entity：持久化模型  
   - DTO：入参 / 服务间传输  
   - VO：对外返回视图  
   禁止互相混用或直接把 Entity 暴露为 API 响应（无明确例外约定时）。

---

## 测试与验证

9. **核心逻辑需要测试**  
   核心业务与关键路径必须有自动化测试覆盖。

10. **修改完成后必须执行测试**  
    改完相关代码后运行对应测试（或约定的测试命令），并依据真实输出判断结果。

11. **不允许伪造测试结果**  
    禁止谎称测试通过、编造日志/覆盖率，或未实际执行却报告成功。

12. **不允许为了测试通过删除测试**  
    禁止通过删除、跳过或弱化断言来“让测试变绿”；应修复实现或正当更新测试。

13. **不允许伪造压测数据**  
    压测/性能相关数据必须来自真实执行；禁止编造 QPS、延迟、吞吐等指标。

---

## 执行原则（摘要）。

- 只做被要求的事；不做未要求的事。  
- 先读再改；少依赖；窄改动。  
- 分层清晰；测真跑；结果真报。
